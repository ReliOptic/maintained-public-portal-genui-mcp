# Draft PR: v0.2 Adapter Discovery And Regional Evidence

Target base: `main`

## Summary

- Adds `DataSection` output to benefit search responses.
- Adds `resource://adapters/v1` discovery for available, unavailable, and
  parked adapters.
- Adds fixture-backed regional evidence for Daejeon Yuseong without ranking that
  data as benefits.
- Exposes adapter discovery as an MCP resource while preserving existing tool
  semantics.
- Updates the demo UI to show adapter readiness, regional evidence, and the
  newlywed freelancer move scenario.
- Documents release readiness and the korean-law decision gate.

## Safety Boundaries

- No live data.go.kr calls are added.
- No korean-law live calls, OC key handling, copied legal text, or legal
  eligibility decisions are added.
- `ev-chargers-kr` stays `unavailable`.
- `korean-law-evidence` stays `parked`.
- Version `0.2.0` is a local proposal until release approval.

## Verification

Use Codex runtime Node `v24.14.0` with pnpm `9.15.9`.

```sh
PATH="/Users/reliqbit_mac/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" pnpm rebuild better-sqlite3
PATH="/Users/reliqbit_mac/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" pnpm schemas
PATH="/Users/reliqbit_mac/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" pnpm build
PATH="/Users/reliqbit_mac/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" pnpm typecheck
PATH="/Users/reliqbit_mac/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" pnpm test
```

Observed passing test suites:

- schema: 4 tests
- core: 19 tests
- demo-ui: 1 test
- mcp-server: 2 tests
- browser-assist: no tests, `--passWithNoTests`

## Risk And Follow-Up

- MCP resource tests cover serialization, not a full client read-resource
  protocol roundtrip.
- Live public-data adapters still require server-side key handling and
  unavailable-state UX.
- Korean-law Option C is prepared as a draft decision package, not applied.

## Outward Commands, Pending Approval

```sh
git push -u origin codex/v0.2-teammate-schema-integration
gh pr create --base main --head codex/v0.2-teammate-schema-integration --title "v0.2 adapter discovery and regional evidence" --body-file docs/v0.2-pr-draft.md --draft
```
